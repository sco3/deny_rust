[pytest]
# Pytest configuration
pythonpath = ..
addopts = -v
# Benchmark configuration (use --benchmark-only flag explicitly for benchmark tests)
# --benchmark-min-rounds=10000
# --benchmark-max-time=0.5
# --benchmark-warmup=off
# --benchmark-disable-gc
# --benchmark-autosave
# --benchmark-sort=name
